# Tester baselines — Postern

> No active phase. When a phased flow resumes, the first gate populates this file.
>
> **Current baseline (commit `b302b3d`, 2026-05-12):** **281 RUN / 279 PASS / 0 FAIL / 2 SKIP** under `make check` (`go vet`, `go test -race`, gofmt-check, tidy-check). Top-level top-line tally: 209 RUN / 207 PASS / 0 FAIL / 2 SKIP. Two pre-existing env-gated SKIPs: `TestConfiguredIDPAccessTokenProviderIntegration`, `TestKeychainIntegration`.
>
> The engineer-CLI phase added 130 new tests across `internal/atomicfile`, `internal/certcache`, `internal/sshconf`, and `pkg/cliapp` (cache, mint, ssh, scp, addhost, removehost, paths, sshuser). Per-package top-level breakdown: `internal/atomicfile` 5, `internal/certcache` 17, `internal/sshconf` 10, `pkg/cliapp` 82, others unchanged (`internal/audit` 5, `internal/broker` 7, `internal/brokerclient` 2, `internal/idp` 5, `internal/oauthlogin` 14, `internal/policy` 7, `internal/ratelimit` 8, `internal/registry` 4, `internal/signer` 1, `internal/tokenstore` 10, `pkg/brokerhandlers` 32).

## Last gate

2026-05-14 TN-E close (tunneling phase-close baseline — final gate of the phase): untagged 551 RUN / 0 FAIL / 2 SKIP; tagged-only (`-tags timefix_test_path ./cmd/timefix-apply/...`) 36 RUN / 0 FAIL / 0 SKIP — combined `make check` RUN = 587 (+1 vs TN-F's 586 from the new `TestTunnelingEndToEnd_HappyPath` cross-package integration test in `internal/securetunnel/integration_test.go`). `make check` exit 0 (fmt-check + vet + vet-with-tag + `go test -race` + tagged `go test -race` + tidy-check all clean); fresh `go test -v -count=1 ./...` and `go test -tags timefix_test_path -v -count=1 ./cmd/timefix-apply/...` `=== RUN Test` counts confirm 551 + 36. Two pre-existing env-gated SKIPs unchanged (`TestConfiguredIDPAccessTokenProviderIntegration`, `TestKeychainIntegration`). Flake-detection: `go test -race -count=3 ./internal/securetunnel/...` green (~16.7s wall combined across 3 iterations) — the new integration test wires brokerclient + brokerhandlers + TunnelIssuer pipeline + V3 WebSocket fake + source-proxy + loopback TCP echo, the highest-concurrency surface on the gate; clean under -race. Terraform validate + `terraform fmt -check -recursive` on `terraform/postern-broker/` exit 0 (one pre-existing benign warning about `aws_region` undeclared var in operator `terraform.tfvars`, not config-affecting). Cross-compile spot-check: `GOOS=windows GOARCH=arm64 go build ./cmd/postern` produces a clean `PE32+ Aarch64 for MS Windows` binary — LD-119 / `.goreleaser.yaml` windows/arm64 addition does not regress. Spot-checks: `TestTunnelingEndToEnd_HappyPath` (`internal/securetunnel/integration_test.go:60`, new this gate) drives the full firewalled-device path through real HTTP (httptest.Server hosting brokerhandlers.New mounted on real *broker.TunnelIssuer with the production pipeline preambles) → brokerclient.OpenTunnel → StartSourceProxy → V3 fake WebSocket handshake → loopback TCP echo round-trip, then asserts the authorized+issued audit pair joined on JTI and the **source-token-never-in-audit invariant T** by JSON-marshaling every event and substring-scanning for the canned token; one behavior per test (the cross-package wiring path is a single integration invariant), no `time.Sleep` as readiness — uses `goleak.VerifyNone` in t.Cleanup + WebSocket handshake completion as the readiness signal, A/A/A clear (fake setup → wire real handler+client → drive client→broker→fake→client round-trip → assert), independent (per-fixture state); `TestTunnelOpenHappyPath` (`pkg/cliapp/tunnel_open_test.go:152`, TN-F) locks the CLI-side lifecycle — tunnelDial composes mint + broker.OpenTunnel + source-proxy start, UpsertEphemeral writes the transient block with sentinel + Port 40404, cancel triggers RemoveEphemeral, proxy Close=1/Wait=1 asserted post-cancel; one behavior, `waitForUpsert` substring-poller as readiness (no `time.Sleep` as readiness), A/A/A clear, independent (per-`t.TempDir()` HOME); `TestTunnelIssuerHappyPath` (`internal/broker/tunnel_test.go:75`, TN-A) locks the broker pipeline success path — registry + policy + AWS-call → authorized+issued audit pair joined on jti with PrincipalType=ModeTunnel, response carries TunnelID/SourceAccessToken/Region/resolved-TTL; one behavior (success path with audit-pair invariant), no sleep, A/A/A clear, independent (newTunnelTestDeps per-test fixture).

2026-05-14 TN-F close: untagged 550 RUN / 0 FAIL / 2 SKIP; tagged 36 RUN — combined 586. Delta vs TN-D (+40 untagged) covered TN-F engineer adds across `pkg/cliapp/tunnel_open_test.go`, `internal/sshconf/ephemeral_test.go`, `pkg/cliapp/add_host_tunnel_test.go`, `pkg/cliapp/mint_state_aware_test.go`, plus F-TN-F-1 `TestReapStaleEphemeralSkipsCorruptRestoreContinuesSweep` + F-TN-F-2 prefix-collision regression suite pinning the `indexLineStart` delimiter fix at `Has`/`Upsert`/`Remove`/`UpsertEphemeral`/`Drop`/`Reap`. Flake-free under `-race -count=3` on sshconf + cliapp.

## Flake registry

| date | test name | failure mode | other tests in same file affected |
|------|-----------|--------------|-----------------------------------|
|      |           |              |                                   |

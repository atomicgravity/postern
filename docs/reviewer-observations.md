# Reviewer observations — Postern

> No active phase. When a phased flow resumes, this file is reinitialized for the new phase.
>
> **Carry-over:** 81 prior observations archived at `docs/archive/{cleanup-phase,impl-pass,p3,oauth2,p4,engineer-cli}/reviewer-observations.md` + 5 candidates from the AWS-deployment increment at `docs/archive/aws-deployment/reviewer-observations.md`. All triaged. Do NOT re-raise as new findings.
>
> **Engineer-CLI phase summary (closed 2026-05-12):** 20 observations raised across the 6 sub-phases; 14 addressed in-phase; 6 carry forward as backlog (obs 4 ed25519 dead fallback, 8 mint double-disk-read, 9 ErrTunnelNotImplemented process-vocab leak, 12 mintAndCache return-cert asymmetry, 15 sshconf dir-chmod layering, 16 narrow Include matcher, 18 DESIGN.md "phase" leak, 19 cache subcommand clock injection, 20 remove-host double-parse). Address opportunistically as nearby work brings them into scope.

## Items

| id | first raised | item | status |
|----|--------------|------|--------|
| 1  | timefix/TF-A | Audit-event struct duplication between authorized + denial paths in `internal/broker/issuetimepayload.go` (two near-identical `AuditEvent{...}` literals at lines 121-129 and 233-246, plus the denial field-stamping pattern repeated at every early-return). Future opportunity: extract a small `newDenialEvent(now, request, id) AuditEvent` constructor + a `newAuthorizedEvent(now, request, engineer, device, id) AuditEvent` constructor in the same file. Same shape would simplify the symmetric cert-mint pipeline if rolled in. Not urgent — pattern duplication, no defect. | open |
| 2  | timefix/TF-A | `denial.EngineerSub/Email/Groups/DeviceSerial` re-assignment inside the signer-failure branch (`issuetimepayload.go:259-262`) re-stamps fields already set earlier in the same execution path. The template `denial` value has those fields populated by lines 138-140 and 184 by the time the signer call runs. Defensive but redundant — flag as future cleanup once the constructor pattern in obs 1 lands. | open |
| 3  | timefix/TF-A | `RecordTimePayloadHandlerDenial` (issuetimepayload.go:296-318) and `RecordHandlerDenial` (cert-mint side, in sshcert.go) are now twins with only the Event literal + PrincipalType field differing. Likely the second handler-denial method that comes through this seam — a third endpoint (tunneling) would land here too. Future opportunity: generalize to `recordHandlerDenial(ctx, event, principalType, denial)`. Out of scope for TF-A but worth flagging before TF-D introduces another endpoint handler. | open |
| 4  | timefix/TF-A | Time-payload denial-reason taxonomy reuses `DenyReasonMissingDeviceID` / `DenyReasonDeviceNotFound` / `DenyReasonRegistryUnavailable` / `DenyReasonPolicyUnavailable` / `DenyReasonAuthorizationDenied` / `DenyReasonRateLimitExceeded` from the cert-mint pipeline (types.go:79-104). Spec §D7 lists these as shared cross-endpoint literals, but the cross-endpoint reuse is only documented for `DenyReasonSignerFailure` (types.go:101-104 comment). Suggest extending the cross-endpoint comment block to enumerate the shared set so a future contributor extending one endpoint's deny vocabulary doesn't accidentally diverge the literals on the other side. | open |
| 5  | timefix/TF-A | `timePayloadHeaderJSON` is hand-rolled at `issuetimepayload.go:50` as a string literal rather than via `json.Marshal` of a typed struct. Comment names the rationale ("fixed at compile time per invariant R so map iteration order can't add a field") but no compile-time check guarantees the string parses as `{"alg":"EdDSA","typ":"postern-timefix+jwt"}` exactly. Future hardening: add a `TestMain` or init-time `json.Unmarshal`-into-strict-shape check that fails if the literal drifts. Low priority — the constant is short and reviewed; this is belt-and-suspenders. | open |
| 6  | timefix/TF-A | Two near-identical deny-template builders now live side-by-side: `certDenialTemplate(engineerCtx)` in `sshcert.go:263-273` and `timePayloadDenialTemplate(engineerCtx)` in `issuetimepayload.go:252-262` are byte-for-byte identical (same six fields populated from `engineerContext`, same shape). The refactor introduced them to centralize the pre-resolveDevice fall-through path. Future opportunity: hoist into a single `engineerDenialTemplate(engineerCtx) AuditEvent` in `preamble.go` (or as a method on `engineerContext`) so the third pipeline that lands (tunneling) doesn't fork a third copy. Not urgent — pattern duplication, no defect. Pairs naturally with obs 1's constructor extraction. | open |
| 7  | timefix/TF-A | `recordCertDenial` / `recordTimePayloadDenialFor` are also twins (sshcert.go:279-285 / issuetimepayload.go:268-274) — both stamp `Event` + `DeniedReason` + `DeviceIDUsed` + `PrincipalType` and dispatch to a `recordDenial`-shaped helper. Only differences: the literal `Event` value, the `PrincipalType` source (`request.PrincipalType` vs hardcoded `ModeTimefix`), and the request-type type-parameter. Generalizable to a single helper taking `(event, deniedReason, deviceID, principalType, eventLiteral)` once a third endpoint forces the issue. Supersedes obs 3 with the post-refactor shape. | open |
| 8  | timefix/TF-A | `engineerContext.ID broker.ID` extension over architect's API sketch is **DISCLOSED-DRIFT** (engineer flagged in handoff). The 6th field carries the full UUID+Serial pair so the SSH cert Serial and audit JTI share one `NewID` call — a real correctness invariant for the audit↔cert join. The spec's `JTI string` is preserved unchanged (strict superset). Routes to architect for non-objection at the next gate-step; not a reviewer block. | flagged-shape |
| 9  | timefix/TF-A | The two `verifyEngineer` and `resolveDevice` preamble methods are defined as `*SSHCertIssuer` receivers (preamble.go:105, 168, 230) even though their bodies access only `i.deps.Clock / IDs / TokenVerifier / RateLimiter / Registry / Policy / Audit`. The spec D9 calls them "pure functions" but the code uses method-on-issuer for dep access. This is fine and idiomatic Go (no point introducing a deps-only struct), but the spec wording vs. code shape disagrees — minor doc drift. If the design wants the preambles testable in isolation, the receiver-method shape blocks it (tests would have to wire a full `SSHCertIssuer`). Recommend either updating the spec wording to "pipeline methods" or factoring deps into a smaller struct if independent unit-test surface is desired later. | open |
| 10 | timefix/TF-B | `verify_test.go` carries a dead helper `readAllOrFatal` kept alive only by `var _ = readAllOrFatal // reserved for follow-on integration tests` at the package level (`verify_test.go:417-426`). This is dead code today — no caller in TF-B — and the "reserved for future" comment is exactly the speculative-helper anti-pattern the code-norms doc calls out (extract on the second caller, not the first). Cleanup: drop the helper + the package-level `_ =` line; reintroduce when TF-D or TF-E actually has a caller. Same critique applies to the `otherPub` capture at `verify_test.go:133-134` (`otherPub, otherPriv, _ := ed25519.GenerateKey(...); _ = otherPub`) — `otherPub` is generated but never used, only `otherPriv` is consumed; replace with `_, otherPriv, _ := ed25519.GenerateKey(...)`. Both are micro-issues, non-blocking. | open |
| 11 | timefix/TF-B | `verify.go:198` rejects when `parsedNow.Before(nowMinBound) || !parsedNow.Before(nowMaxBound)` — i.e. inclusive-lower / exclusive-upper. The verifier source comment documents this choice ("Inclusive lower, exclusive upper"). The spec text §"Device-side validation" check #7 + invariant P + TF-C arg-range talk about `(2024, 2099)` and "between 2024 and 2099" without committing to half-open vs. open vs. closed semantics. Both endpoints are deep into the future/past relative to any realistic `now`, so the practical difference is nil, but the spec and TF-C setter (which the architect-log notes will share the same bound check) should explicitly say "[2024-01-01T00:00:00Z, 2099-01-01T00:00:00Z)" so TF-C's sanity-range check doesn't pick the opposite half-open shape and diverge by one second on the 2099 boundary. Spec-clarification task for the architect; not a TF-B defect. | open |
| 12 | timefix/TF-B | `main.go:121-123` flushes the nonce via a type-assertion to `*os.File` + `Sync()`. The pattern works for production (`d.Stdout = os.Stdout`, which is `*os.File`) and is a no-op in tests (the `bytes.Buffer` doesn't match the assertion, so the flush is silently skipped). The implicit assumption is "any non-`*os.File` writer doesn't need explicit flushing" — true for the in-tree test stubs but the contract isn't named in the deps comment. Future hardening: either document this on the `Stdout io.Writer` field comment, or define an explicit `Flusher` seam on the deps struct that tests can opt into. Non-blocking. | open |
| 13 | timefix/TF-B | The verifier loads + parses + downcasts the CA pubkey on every invocation (`main.go:95`, `loadCAPubKey` at `:184`). This is correct for an `Exec`-replaced short-lived process — there's no warm cache to populate — but a future temptation will be "cache the parsed pubkey in a package-level `sync.Once` to skip the parse on retries." Resist: the process is one-shot, the file read + parse cost is negligible against `clock_settime`, and the `sync.Once` would bake in the pubkey path resolution at first-call time (interacting badly with the test-build env-var override). Flag as anti-temptation rather than defect. | open |
| 14 | timefix/TF-B | The `readJWS` size-cap implementation uses `io.LimitReader(r, MaxJWSBytes+1)` + `io.ReadAll` + `len(buf) > MaxJWSBytes` check (`main.go:166-176`). Correct, and the `+1` ceiling captures the over-cap case exactly. Worth a brief inline comment naming why the `+1`: future contributors removing it ("LimitReader of `MaxJWSBytes` is enough, isn't it?") would silently change the boundary semantics from "reject if more than 4096" to "accept exactly 4096 but truncate at 4096 if more was sent" — the latter swallows the attack signal. The current comment block above the function says "Going past the cap is treated as bad input" but doesn't explicitly tie that to the `+1` mechanic. Minor doc-clarity tweak. | open |
| 15 | timefix/TF-C | `setRTC` populates the `unix.RTCTime` `Wday`/`Yday`/`Isdst` fields (`syscalls_linux.go:55-58`) even though the inline comment notes "the kernel itself ignores Wday/Yday/Isdst on SET". The defensive population matches the documented `struct rtc_time` contract but is dead-write — and `time.Weekday()` returns `Sunday=0..Saturday=6` while the kernel's expectation (per `linux/rtc.h`) is documented as the same `Sunday=0` convention, so the values match by coincidence rather than by audit. Future contributor temptation: "should `Wday` use Monday=0 like ISO 8601?" — currently no test or comment pins the convention. Low priority; the kernel ignores the field. Either drop the dead writes with a one-line comment or pin the convention with an assertion test if kept. | open |
| 16 | timefix/TF-C | `syscalls_other.go` returns `errUnsupportedPlatform` from both seams; on macOS the binary deterministically exits 3 with a stderr line (verified). This is the correct shape, but the test suite never exercises the `syscalls_other.go` path — `main_test.go` always stubs the seams via `deps`. The `_other` file is reachable only via `GOOS=darwin go run` or hand-build, and a future contributor refactoring the seam shape could silently make the fallback a no-op (e.g. accidentally returning nil) without a test failure. Cheap regression guard: a `TestPlatformBuildTagReturnsError` that on `runtime.GOOS != "linux"` calls `setKernelClock` / `setRTC` from `syscalls_other.go` directly and asserts non-nil error. Optional — the cross-compile in `make check`'s `go vet` already exercises compilation. | open |
| 17 | timefix/TF-C | The half-open bound check (`main.go:120`) reads `target.Before(tsMinBound) || !target.Before(tsMaxBound)`. Functionally correct and the test table at `main_test.go:106-147` walks both endpoints with named rows. The double-negative `!target.Before(tsMaxBound)` is idiomatic Go but takes a beat to parse; the verifier (`cmd/timefix-apply/verify.go:198`) uses the same shape so the codebase is consistent. If a future cleanup sweep wants symmetry with the alternative formulation `target.After(tsMinBound.Add(-time.Nanosecond)) && target.Before(tsMaxBound)`, prefer staying with the current double-negative — it's the verifier's shape, and consistency across the two binaries beats local readability. Flag as anti-temptation rather than defect. | open |
| 18 | timefix/TF-C | `examples/on-device/timefix/README.md`'s "Verifying end-to-end" section ends with `sudo -u timefix /usr/sbin/timefix-set-clock 1747000000` and notes "this advances the device's kernel clock to 2025-05-11T22:13:20Z". The example timestamp `1747000000` is hardcoded in both the README and three test cases (`main_test.go:154,178,205`). The README's "do this only on a device you don't mind perturbing" warning is correct, but the verification example would be friendlier if it used a `date +%s` substitution (the engineer's *current* time) rather than a static 2025 timestamp — using `1747000000` on a device that *already* has the correct clock advances it backward, which the half-open bound check accepts (2025 ≥ 2024) but is operationally confusing. Suggest `sudo -u timefix /usr/sbin/timefix-set-clock $(date +%s)`. Doc-polish, non-blocking. | open |
| 19 | timefix/TF-C | The setter has no `--version` / `--help` surface — per invariant O this is correct (`-v` would be parsed as wrong-arg-count first). However, this means standard operational discovery tooling (e.g. an SRE running `which timefix-set-clock && timefix-set-clock --version` to confirm a release version landed) does not work. The verifier (TF-B) has the same constraint. Operator workaround is `file /usr/sbin/timefix-set-clock` + GoReleaser metadata embedded in the binary, or `dpkg -l` / `rpm -qa` for packaged builds. Not a defect — invariant O is load-bearing and the absence is the point — but worth a one-line note in `examples/on-device/timefix/README.md` so the next operator doesn't try `--version` and conclude the binary is broken. | open |
| 20 | timefix/TF-D | `defaultExecSSHTimefix` (`pkg/cliapp/runtime.go:148-203`) wraps an *exec.ExitError twice on a clean device-side non-zero exit: the wait branch returns `fmt.Errorf("execSSHTimefix: ssh exited with code %d", exitCode)` *and* the `exitCode` is propagated as the first return value. `runTimefix` then wraps a third time: `fmt.Errorf("timefix on %q failed (device exit %d): %w", deviceID, exitCode, err)`. The engineer sees the exit code mentioned twice ("timefix on ... failed (device exit 5): execSSHTimefix: ssh exited with code 5"). Minor cosmetic — consider stripping the inner "ssh exited with code %d" wrap when `exitCode` is already in the return tuple, so the caller's wrap is the only one that quotes the number. Non-blocking; surfaces a coherent error today. | open |
| 21 | timefix/TF-D | The streaming exec discards a buffered-reader's internal buffer immediately after `ReadString('\n')` (`pkg/cliapp/runtime.go:175`). The child's stdout buffer beyond the first line — if the device's verifier emits additional diagnostics on stdout between nonce-emit and the verifier's eventual exit (unlikely given the device side's privilege-split design, but not architecturally forbidden) — is dropped on the floor rather than tee-mirrored to the engineer's stdout. Today the device's verifier writes diagnostics to stderr (which is wired through verbatim) and the protocol pins stdout to a single nonce line, so the discard is correct for the v1 wire shape. Flag for the v2 case if the device wire ever widens to multi-line stdout. Non-blocking. | open |
| 22 | timefix/TF-D | `validateTimefixNonce` (`pkg/cliapp/timefix.go:209-222`) duplicates the broker's nonce shape check (32-byte base64url-decoded). The broker's check lives in `internal/broker/issuetimepayload.go`. The duplication is intentional per the docstring ("Catching here means a malformed-device-output error is distinct from a broker rejection") and saves a round-trip, but the byte-count constant `timefixNonceDecodedBytes = 32` is defined twice across the codebase — if a future protocol revision moves to 64-byte nonces both sides need updating. Future cleanup: hoist the constant into `internal/broker` and import on the CLI side, or pull it from a shared `brokerwire`-style package. Pairs with `internal/broker/issuetimepayload.go`'s analogous constant. Non-blocking, ~3-line cleanup whenever the broker-side nonce-shape constants get reorganized. | open |
| 23 | timefix/TF-D | `writeTimefixCertTempfile` (`pkg/cliapp/timefix.go:179-203`) opens with `os.CreateTemp("", ...)` which on POSIX creates with `0600` already; the explicit `Chmod(0o600)` is defensive. Reviewer-observed but harmless — the chmod is a no-op on the production path. Worth a one-line comment naming why the chmod stays (defense against future Go-runtime behavior change on Windows where `CreateTemp` semantics differ from POSIX) or dropping the call entirely. Non-blocking. | open |
| 24 | timefix/TF-D | The supplier-error path in `defaultExecSSHTimefix` (`pkg/cliapp/runtime.go:184-188`) closes stdin and `_ = cmd.Wait()`s without surfacing the wait result. Practically fine — the supplier error is the proximate cause and the caller already gets it. But on the SSH side, closing stdin makes the child's blocking read return EOF; the device verifier then exits non-zero (bad-input). The non-zero exit error from cmd.Wait is silently swallowed. Today this is correct (the supplier error is more informative) but if a future contributor ever wants to assert "stdin close + device verifier got EOF cleanly" they'd need to thread the wait error through. Document the swallow with a one-line comment naming why the wait error is intentionally dropped here. Non-blocking. | open |
| 25 | timefix/TF-E | `defaultExecSSHTimefix` reads the device-clock via `bufio.NewReader.ReadString('\n')` twice and never tee-mirrors the read bytes onto the engineer's stderr/stdout. The supplier callback receives the `deviceClock` string and the verbose-path `verbosef` line prints it (`pkg/cliapp/timefix.go:153`). However, when `--quiet` is set the device-clock value is fetched, parsed, and then thrown away — the read still happens (required for the wire protocol), but the value the verifier emitted never reaches the engineer. Today this is intentional (D15: `--quiet` suppresses progress, and device-clock is engineer-facing diagnostic only), so no defect. Worth flagging as anti-temptation: a future contributor refactoring "we read the clock but never use it under --quiet" might be tempted to skip the read entirely; doing so would break the wire protocol because the verifier emits exactly two lines and stdin-side JWS writes only start after both have been drained. The two-line read is load-bearing for wire-format synchronization, not for diagnostic surfacing. Non-blocking. | open |
| 26 | timefix/TF-E | `defaultExecSSHTimefix` integration test (`cmd/timefix-apply/integration_test.go`) compiles only under `-tags timefix_test_path` and runs as part of the tagged variant in `make check`. The tagged-variant gate was originally introduced (LD-69) to keep the env-var CA-pubkey override seam out of production builds; bundling the integration test there is opportunistic and works today. Future watch-item: if a new integration test wants both the env-var seam AND a different gating shape (e.g. `-tags integration` to skip on `go test -short`), the two concerns will need separate tags rather than both riding on `timefix_test_path`. Non-blocking; document the conflation if a second consumer of `timefix_test_path` lands. | open |
| 27 | timefix/TF-E | `cmd/timefix-apply/integration_test.go:108-138` reads verifier stdout via a single `Read` loop on the bytes side and a `WaitGroup` of size 1 — the wg add+wait around a single goroutine is a no-op (no concurrency benefit; the goroutine runs to completion before `wg.Wait()` returns because that's the only goroutine the wg tracks). The shape works because the loop bails when it sees two newlines, but the wg gives the impression of concurrent stdout-draining that isn't there. Future cleanup: drop the wg or replace the goroutine with an inline loop. Non-blocking, ~5-line simplification. | open |
| 28 | timefix/TF-E | Two residual `invariant [A-Z]` references remain in production code at `cmd/timefix-apply/verify.go:66` ("invariant-P purposes") and `cmd/timefix-apply/verify.go:178-179` ("per invariant R forward-compat rule"). The CV-1 sweep across `internal/` + `pkg/` + the rest of `cmd/` cleaned every LD-X / TF-X / invariant-letter ref in production code; only these two letter-refs in verify.go survived. Spec §8 success criteria #16 calls for the production-code reference count to go to zero (with the note that invariant-letter refs are weaker but still in scope). Cheap follow-up: inline the underlying property at each site ("bad-input exit class" instead of "invariant-P purposes"; "the wire format pins exactly two header fields; unknown payload fields are forward-compat headroom" instead of "per invariant R forward-compat rule"). Non-blocking — the residue is minimal and the rationale is already inlined in surrounding sentences; sweeping the two letter-refs is a one-commit cleanup. | open |
| 29 | timefix/TF-E | Test-file CV-1 refs survive per the spec's "lower priority — sweep if cheap, defer if not" disposition. Verified surviving in: `cmd/timefix-set-clock/main_test.go:45,101,171,182` (invariant O, LD-85, LD-77 ×2), `cmd/timefix-apply/verify_test.go:126,128,229,356,388,439` (invariant P ×4, invariant N, LD-69), `pkg/cliapp/timefix_test.go:36,68,578` (D12 references — these read naturally as design-spec citations rather than phase identifiers, since "D12" is a stable design-doc decision label). Three rough buckets: (a) invariant-letter refs in tests are reasonable when they name the *invariant being tested* rather than an opaque label (an "invariant P test table" describes what it does); (b) LD-X refs in tests should generally be inlined to the underlying property; (c) "D12" refs are spec-section citations and the cleanest of the three categories. Future cleanup: prefer (a)+(c) when retaining is justifiable; sweep (b). Non-blocking. | open |
| F-TN-A-1 | tunneling/TN-A | **Resolves F-BRK-M3** — `/ssh/tunnel` is now fully wired (POST-only via method-prefixed route, bearer-token-required, body-size-capped, JSON `DisallowUnknownFields`, full audit pipeline via `RecordTunnelHandlerDenial` + the tunnel-open issuer pipeline). Verified by handler tests `TestTunnelRejectsNonPOSTMethods`, `TestTunnelRequiresBearerToken`, `TestTunnelAuditsMalformedRequestBody`. The 501-when-unwired path is preserved (auditless-by-design wrapping-misconfiguration signal). | resolved |
| F-TN-A-2 | tunneling/TN-A | `pkg/brokerhandlers/handlers.go` — `writeIssueError` hardcoded `"ssh cert issuer"` text leaked into `/ssh/time-payload` and `/ssh/tunnel` 500 responses and slog lines. **Resolved in-gate** per architect F-TN-A-2 ruling: `writeIssueError(response, routeLabel, err)` parameterized; three call sites pass `"ssh cert issuer"` / `"time-payload issuer"` / `"tunnel issuer"`; pinned by `TestWriteIssueErrorLabelsPerRoute` (one sub-case per route asserting the label appears in the 500 body). | resolved |
| F-TN-A-3 | tunneling/TN-A | `internal/broker/tunnel.go` — PolicyContext comment described "engineer requested" semantics but code injects the resolved (post-default-substitution) value. **Resolved in-gate**: comment rewritten to describe the resolved-value semantics + bypass-prevention rationale (omitting the flag must not satisfy `when context.requested_max_lifetime_minutes <= N` ceilings); `TestTunnelIssuerPolicyContextLifetimeUsesResolvedDefault` added covering the zero-request → default-substitution path. | resolved |
| F-TN-A-4 | tunneling/TN-A | `internal/broker/tunnel.go` — `tunnelingDenyReason` + `mapTunnelingError` used direct type assertion on `TunnelingErrorClassifier`. **Resolved in-gate** per LD-103 shape ruling: switched both to `var classifier TunnelingErrorClassifier; errors.As(err, &classifier)` matching the `policyDenyReason` / `registryDenyReason` pattern in `preamble.go`; pinned by `TestTunnelingErrorClassifierSurvivesErrorWrapping` (wraps a classifier-implementing error via `fmt.Errorf("%w", ...)` and asserts the 429 + `tunnel_limit_exceeded` mapping survives). | resolved |
| F-TN-A-5 | tunneling/TN-A | `internal/policy/avp_test.go` — missing regression guard for the Context-overlay reserved-key shadow defense. **Resolved in-gate**: `TestAllowContextOverlayDoesNotShadowReservedKeys` added asserting that a `PolicyRequest.Context` map setting `source_ip` / `request_id` / `timestamp` / `user_agent` to attacker values does NOT shadow the broker-resolved baseline. | resolved |
| F-TN-B-1 | tunneling/TN-B | `internal/securetunnel/frame.go:48-50` — the `length > maxFramePayload` guard in `framedReader.next` is unreachable in the current wire shape: the 2-byte big-endian length prefix caps each envelope at 65535 bytes (uint16 max), well below `maxFramePayload = 131076`. The 131076 ceiling IS enforced separately at the WebSocket layer via `conn.SetReadLimit(maxFramePayload + 8)` (proxy.go:192), so oversized incoming WebSocket frames are still rejected before reaching the framedReader — the dead branch is defense-in-depth, not a defect. The `TestFramedReader_TruncatedFrameSurfacesError` test comment honestly notes this ("the 2-byte length prefix can only represent up to 65535 bytes which is below the protocol-documented per-Message ceiling, so the maxFramePayload guard in framedReader.next is defensive (would only fire if the protocol ever widened to a 4-byte prefix)"). Future cleanup: either drop the check + add a one-line comment naming "uint16 length prefix bounds envelope to 65535; WebSocket frame size is bounded separately by SetReadLimit at the dial site" at the dial-site SetReadLimit call, or keep the check and inline a comment naming it as defense-in-depth. Pairs naturally with TN-E's success-criteria-#7 test for oversized incoming frames — that test will need to drive the WebSocket layer with a >131076-byte frame to hit the live ceiling, not the dead 2-byte-prefix path. Non-blocking. | open |
| F-TN-B-2 | tunneling/TN-B | `internal/securetunnel/proxy.go` — `awaitServiceIDs` lacked an internal handshake deadline; a misbehaving destination that opens the WebSocket but never emits SERVICE_IDS could hang `StartSourceProxy` indefinitely. **Resolved in-gate** per LD-106: added `SourceProxyOptions.HandshakeTimeout time.Duration` with a 10-second package-internal default; `awaitServiceIDs` wraps its read in `context.WithTimeout`. Caller-supplied ctx cancellation still composes. Pinned by `TestStartSourceProxy_HandshakeTimeout` (`HandshakeTimeout: 50ms`, asserts `errors.Is(err, context.DeadlineExceeded)` within 2s). | resolved |
| F-TN-B-3 | tunneling/TN-B | File-size targets per `code-norms.md` (~200 code-line target) — `internal/securetunnel/proxy.go` is 307 lines mixing types (`SourceProxy`, `SourceProxyOptions`, `stream`), lifecycle (`StartSourceProxy`, `Close`, `Wait`, `terminate`), and one-off setup (handshake-await, listener-bind); `internal/securetunnel/loops.go` is 295 lines bundling the read loop, dispatch switch, accept loop, per-stream pump, frame emit, stream lookup, and stream-id allocator. Both overshoot, and the concerns inside each are visually distinct: a future cleanup that splits `proxy.go` into `proxy.go` (types + lifecycle) + `handshake.go` (dial + await-service-ids + terminate-on-handshake-failure) and `loops.go` into `read.go` (read loop + dispatch) + `streams.go` (accept + per-stream pump + stream-id allocator + closeStream/lookupStream) would carry the same code without re-architecting and would reduce the per-file scan cost on the next reader. Non-blocking; defer until TN-E's docs/cleanup pass or the next nearby change brings the files into scope. | open |
| F-TN-B-4 | tunneling/TN-B | Spec §"D12" / `docs/phases/tunneling/spec.md:58` names a three-goroutine-per-tunnel concurrency model: "two goroutines per tunnel — one reads from the local TCP socket and writes DATA messages to the WebSocket; the other reads DATA messages from the WebSocket and writes to the local TCP socket. A third coordinator goroutine handles control-message dispatch." Implementation diverges: `proxy.wg.Add(2)` for `runReadLoop` + `runAcceptLoop` at TN-B startup, plus a per-stream `runStreamPump` per accepted TCP connection (`wg.Add(1)` per accept). Control-message dispatch (`STREAM_RESET` / `CONNECTION_RESET` / `SESSION_RESET` / `SERVICE_IDS` republish) is handled inline inside `runReadLoop` via `dispatchMessage`, not in a separate coordinator goroutine. The implementation shape is sound (and simpler — one fewer channel-coordinated goroutine to leak) and matches the v1 single-stream constraint, but the spec text now disagrees with the code. Spec-clarification task for the architect at TN-B close: update spec §"D12" to name the actual shape (read-loop + accept-loop + N per-stream pumps, control dispatch inline in read-loop), or document why the three-goroutine shape was abandoned. Not a TN-B defect; reviewer-prompted spec-vs-code reconciliation. | open |
| F-TN-C-1 | tunneling/TN-C | **`runTunneledSSH` uses inline Close-then-Wait, not `defer`, so a panic inside `rt.execSSH` (or any code between the starter return and the explicit teardown) skips proxy cleanup.** Spec D13 + D16 + LD-95 name the proxy lifetime as "bounded by the ssh subprocess lifetime"; the engineer's architectural note flags explicit-close-after-exec as intentional for "verbose-mode message ordering" (so a future post-exec verbose line lands after teardown, not interleaved with proxy goroutine drain). The blast radius today is bounded: (a) `defaultExecSSH` is a thin wrapper around `cmd.Run()` and is extremely unlikely to panic; (b) the proxy's underlying goroutines tie to `cmd.Context()` and would drain when the cobra context is canceled on process exit — so leakage is a moderate `Wait()` join skip, not a runaway WebSocket. But the explicit-close vs deferred-close asymmetry is exactly the shape `code-norms.md` warns about: "rely on defer for resource cleanup unless there's a stated reason." The verbose-ordering rationale could be addressed with a deferred Close + an inline final verbose line before return. Future cleanup, once TN-D's shared `tunnelDial` consumer pattern is in place: either move teardown to `defer` and surface verbose lines via a deferred helper, or document the no-defer choice with a one-line comment naming the specific verbose-ordering interaction that drove it. Non-blocking — the lifecycle is correct on every test-covered path; this is robustness-for-rare-failure rather than a defect. | open |
| F-TN-C-2 | tunneling/TN-C | **`ssh` exit-code propagation is collapsed by `cmd/postern/main.go:26` (`os.Exit(1)` on any error) regardless of whether the inner error chain carries an `*exec.ExitError`.** TestSSHTunnelPreservesExitCode asserts `errors.Is(err, sshErr)` — the error chain is preserved through `runTunneledSSH` → cobra `RunE` — but the engineer's shell still sees exit 1, not ssh's actual exit (255 for connection failure, 130 for SIGINT, etc.). This is a **pre-existing** limitation on the direct-LAN path too (ssh.go's `rt.execSSH` chains the error and main.go maps to 1); TN-C doesn't introduce the gap. The test naming is mildly misleading: it locks the error-chain preservation, not the engineer-visible exit code. Future cleanup whenever the upgrade subcommand phase or another exit-code-sensitive subcommand lands: in `cmd/postern/main.go`, replace the `os.Exit(1)` with `exitCodeFrom(err)` (the helper already exists at `pkg/cliapp/runtime.go:307`) so `*exec.ExitError` exit codes flow through to the engineer's shell — matters for shell pipelines and CI step-result reporting that inspect `$?`. Non-blocking; pre-existing shape, not a TN-C regression. | open |
| F-TN-C-3 | tunneling/TN-C | **`findHostPositional` + `sshFlagConsumesArg` work correctly on the test-covered argv shapes but have NO direct unit tests** — the algorithm is exercised only indirectly through `TestSSHTunnelHappyPath` (one bare `engineer@target-host`) and `TestSSHTunnelAppendsDeviceIDWhenNoHost` (empty passthrough). The interesting cases — `-L 8080:localhost:80 alice@host`, `-oFoo=bar alice@host`, `-vN alice@host` (bundled bool flags), `-l alice host`, `-J jumphost alice@host`, an engineer-supplied remote command starting with `-` — are walked by inspection but not pinned by table-driven tests. The conservative skip-list covers common single-letter flags that consume args; multi-letter forms (`-vvv`, `-oKey=Val`) and bundled flags (`-vN`) take the "len > 2 → not arg-consuming" branch correctly. Future cleanup: add a small `TestFindHostPositional` table directly exercising the algorithm with the half-dozen argv shapes above, so a future engineer extending `sshFlagConsumesArg` (or hitting a real engineer-reported "ssh in `--tunnel` parses my argv wrong" bug) has a regression net. The algorithm is also a candidate for `tunnelDial`-shared reuse in TN-D's `scp --tunnel` path; scp's flag set differs from ssh's (scp has `-S program`, `-l limit`, `-P port` vs ssh's `-l login`, `-p port`) so direct argv-rewrite tests will need parallel scp-flavored variants. Non-blocking. | open |
| F-TN-C-4 | tunneling/TN-C | **Two `TN-D` references survive in production code** at `pkg/cliapp/ssh.go:37` ("through TN-D's transition") and `pkg/cliapp/tunnel.go:32` ("scp once TN-D lands"). Per CV-1 / user Go-style rule, production comments should not reference sub-phase identifiers. Both are mild (naming a *forward-looking* concern rather than a closed phase identifier), but they extend CV-1's footprint into a previously-clean file (tunnel.go is new). Cheap fix in TN-D or TN-E: replace `TN-D` with the underlying property — `ssh.go:37` "Retained as an exported symbol so wrappers that reference it through the scp `--tunnel` rollout do not break"; `tunnel.go:32` "(ssh today, scp once its tunnel-mode counterpart lands)". Non-blocking; cleanup track. **TN-D update**: `ssh.go:37` resolved (sentinel deleted with the surrounding doc-comment per LD-110). `tunnel.go:32` survives unchanged. | partially resolved |
| F-TN-D-1 | tunneling/TN-D | **`scpFlagConsumesArg` skip-list omits `-D` (sftp_server_path) and `-X` (sftp_option).** scp's own usage line (per `scp` invoked with no args: `[-c cipher] [-D sftp_server_path] [-F ssh_config] [-i identity_file] [-J destination] [-l limit] [-o ssh_option] [-P port] [-S program] [-X sftp_option] source ... target`) lists ten arg-consuming flags; the pinned list `{-c, -F, -i, -J, -l, -o, -P, -S}` (LD-111) is missing `-D` and `-X`. Practical impact today is bounded: `-D <path>` would treat the path as a positional and the argv walker would inspect it via `looksLikeRemoteSCPSpec` — a typical `/usr/lib/openssh/sftp-server` argument contains a slash before any colon and is correctly classified local (no rewrite); `-X <key=value>` arguments (e.g. `buffer=1048576`) have no colon and also pass through. The omission is operationally non-fatal for common shapes but defensively incorrect: a contrived `-D` value that *did* look remote (`sftp:server`) would be silently rewritten to `sftp:<device-id>`. LD-111 pinned the smaller set explicitly, so this is either a deliberate exclusion (worth a one-line comment naming why) or a gap. Cheap follow-up: add `-D` and `-X` to the switch and reference scp's usage line in the surrounding doc-comment so the next contributor extending the list has scp's authoritative flag list as the named source. Non-blocking. | open |
| F-TN-D-2 | tunneling/TN-D | **`looksLikeRemoteSCPSpec` + `rewriteSCPHost` do not handle IPv6 bracket syntax** (`[2001:db8::1]:/path` — scp's documented IPv6 spelling). `looksLikeRemoteSCPSpec` finds the *first* `:` and checks for slash-in-prefix; the IPv6 prefix `[2001` contains no slash, so the token is classified remote. `rewriteSCPHost` then splits at the first colon, producing `[2001` as the host portion and `:db8::1]:/path` as the path portion — the bracket-form is mangled into `[2001:` rewritten to `<device-id>:db8::1]:/path`, which scp would not parse as a valid file spec. Practical impact: an engineer running `postern scp --tunnel <device> '[2001:db8::1]:/etc/foo' ./foo` would see scp fail at argv-parse, not at the broker. The IPv6 case is uncommon for embedded-device fleets (usually IPv4 LANs), and the tunnel-mode rewrite-to-device-id is the *whole point* (the engineer's IPv6 address gets discarded anyway), so an alternative defensive fix would be: if `looksLikeRemoteSCPSpec` detects bracket-form, replace the entire `[...]:` prefix with `<device-id>:` rather than splitting on the first colon. Cheapest: add a one-line check `if strings.HasPrefix(token, "[")` in `rewriteSCPHost` that finds the closing `]` + `:` and splits there. Non-blocking — engineer-actionable failure shape and no security implication. | open |
| F-TN-D-3 | tunneling/TN-D | **TN-D introduces 3 new production-code phase-identifier refs in scp.go**: `LD-109` at `:110`, `D3` at `:146`, `LD-109` at `:154`. Per CV-1 / user Go-style rule (`feedback_go_code_style.md`), production comments must not reference LD-X / D-X / phase identifiers. Same pattern as F-TN-C-4 (which TN-D partially resolved by removing the `TN-D` ref at `ssh.go:37`). The three new refs in scp.go are all in doc-comments on the scp argv-builder + walker; each could be inlined to the underlying property (`per LD-109's rationale for ssh` → `for the same reason ssh's tunneled argv-builder preserves the device-id in the host segment`; `flags from D3 generalized to scp` → `flags from the source-proxy ssh argv generalized to scp`; `device-id rewriting per LD-109` → `device-id rewriting`). Cheap one-commit cleanup; pairs with F-TN-C-4's outstanding `tunnel.go:32` ref. Total in-flight CV-1 footprint in cliapp tunnel-mode production code: 4 refs across 2 files. Non-blocking. | open |
| F-TN-F-1 | tunneling/TN-F | **`ReapStaleEphemeral` aborted mid-iteration on a corrupt RESTORE base64, bricking every subsequent `postern tunnel` invocation including `--reap`. Resolved in-gate**: skip-and-continue semantics; reaper logs slog.Warn per failure and returns nil unless catastrophic file-read fails. Pinned by `TestReapStaleEphemeralSkipsCorruptRestoreContinuesSweep` (seeds corrupt + valid ephemerals; asserts valid one is reaped, corrupt left for manual recovery, function returns nil). Original detail kept below for archive:** Walk: an ephemeral block carries a `# POSTERN-EPHEMERAL-RESTORE:` line whose base64 fails to decode (manual file tampering, on-disk corruption, a future Postern version that adds restore-format versioning that an older version rejects). The owning process dies. The engineer's next `postern tunnel` runs the reaper; `ReapStaleEphemeral` (`internal/sshconf/ephemeral.go:208-210`) hits the dead pid, calls `RemoveEphemeral`, which fails with `sshconf: decode RESTORE for %q: %w`. The reaper returns `(reaped, err)` and stops — every subsequent `postern tunnel <any-device>` invocation now fails at startup because `runTunnelOpen` (`pkg/cliapp/tunnel_open.go:102-105`) wraps the reaper error and aborts before reaching `UpsertEphemeral`. **Engineer-visible failure shape**: every `postern tunnel` invocation errors with `reap stale ephemeral stanzas: sshconf: decode RESTORE for "device-X": illegal base64 data`, regardless of which device the engineer asked for. The only escape hatch is hand-editing `~/.postern/ssh.conf` to drop the corrupt block — `postern tunnel --reap` runs the same broken reaper. Defensive fix: have the reaper either (a) skip-and-continue on per-device errors, accumulating them into a returned multi-error so the engineer gets diagnostic info but a single corrupt block doesn't block all tunnel operations, or (b) treat a decode failure as "delete the block" (the persistent stanza is lost in the corrupt-restore case, but the engineer can re-`add-host` it). (b) matches the LD-115 reaper-restores-the-engineer-to-a-usable-state intent; (a) preserves more information. Pairs with a regression test `TestReapStaleEphemeralSurvivesCorruptRestoreLine` that seeds a stale ephemeral block with corrupt base64 + a second valid stale ephemeral block, and asserts the reaper drops at least the valid one. Non-blocking — this is a corner-case crash-safety hole, not a happy-path bug. | resolved |
| F-TN-F-2 | tunneling/TN-F | **`Has()` / `Upsert` / `Remove` / `extractBlock` suffered a device-id prefix collision via `indexLineStart` (line-start anchored, no end-of-token check). Latent since the engineer-CLI phase: `Upsert("device-1")` had been clobbering `device-12`'s block; `Remove("device-1")` deleting it. Resolved in-gate** via single-point fix at `indexLineStart` requiring `\n` or EOF immediately after the matched needle; all callers inherit. Pinned by 5 regression tests (`TestHasRejectsPrefixCollision`, `TestUpsertRejectsPrefixCollision`, `TestRemoveRejectsPrefixCollision`, `TestUpsertEphemeralRejectsPrefixCollision`, `TestRemoveEphemeralRejectsPrefixCollision`) — each verified FAIL pre-fix, PASS post-fix. Original detail kept below for archive:** The line-start check matches the needle `# BEGIN POSTERN-MANAGED: device-1` against the file content `# BEGIN POSTERN-MANAGED: device-12\n` — start-of-line is anchored, end-of-needle is not. Same applies to `extractBlock` and the existing `Upsert`/`Remove` primitives — this is a pre-existing writer-package issue that LD-116's new `Has()` inherits. Practical impact: mint's state-aware output (`pkg/cliapp/mint.go:64-74`) reads true for a device that isn't actually registered, and the engineer sees the registered-branch single-line `ssh <device>` connect hint when they should see the `add-host` suggestion. The connect hint then fails at ssh-time because the device-id doesn't match any actual Host stanza. Same risk affects `UpsertEphemeral`/`RemoveEphemeral` — `RemoveEphemeral("device-1")` could no-op on a stanza for `device-12` (since `extractBlock` finds the wrong block, sees no ephemeral sentinel, and exits at line 164), or trigger a same-device false-collision in `UpsertEphemeral` (sees the ephemeral sentinel from the wrong device, returns `ErrEphemeralConcurrent`). Defensive fix: change the line-start check to a line-start-AND-end check — `indexLineStart` would compare `needle + "\n"` (or `needle + "\n"` / `needle + " "` for tolerance) against `existing[abs:abs+len(needle)+1]`. Or, hoist the marker shape to `# BEGIN POSTERN-MANAGED: <device> #` with a closing sentinel that disambiguates by length. Pairs with a regression test in `internal/sshconf` driving both Upsert and Has against a file containing `device-1`, `device-12`, and `device-100` simultaneously. Non-blocking — the affected device-id pairs need the second to be a strict prefix of the first AND both to be registered, which is uncommon for embedded-device-fleet naming schemes; but the bug surface is broad (every primitive that calls `indexLineStart`). | resolved |
| F-TN-F-3 | tunneling/TN-F | **CV-1 footprint regression in TN-F production code: 8 new LD-XX phase-identifier references across 3 files.** `pkg/cliapp/addhost.go:85,150` (LD-112 ×2); `pkg/cliapp/mint.go:59,63,116` (LD-116, LD-112, LD-116); `pkg/cliapp/tunnel_open.go:76,98,170` (LD-113, LD-115, LD-115); `internal/sshconf/ephemeral.go:56` (LD-115). Per CV-1 / user Go-style rule (`feedback_go_code_style.md`): production comments explain *what and why when non-obvious*, never reference phases / LD-X / sub-phase numbers. Each ref carries useful rationale but the phase-id tag is the wrong form — inline the underlying property. Examples: `mint.go:59` "State-aware branch (LD-116): consult the Postern-managed ssh.conf" → "State-aware branch: consult the Postern-managed ssh.conf"; `tunnel_open.go:98` "Reaper-runs-before-write (LD-115): every invocation cleans up stale ephemeral stanzas before writing its own" → "Reaper-runs-before-write: every invocation cleans up stale ephemeral stanzas before writing its own". Total in-flight CV-1 footprint in cliapp tunnel-mode + addhost + mint production code is now 4 (from F-TN-C-4/F-TN-D-3, of which 1 resolved) + 8 (new) = 11 refs across 5 files. **Test files also carry 12 new LD/TN/OQ refs** in TN-F's new/modified test code (lower priority per spec §"CV-1 sweep" disposition). Cheap one-commit cleanup; pairs with F-TN-C-4 / F-TN-D-3 / verify.go residue. Non-blocking. | open |
| F-TN-F-4 | tunneling/TN-F | **Reaper-before-flag-validation ordering: `--max-lifetime` rejection happens INSIDE `tunnelDial` (line 62), but `runTunnelOpen` runs `openSSHConfWriter` + `ReapStaleEphemeral` (lines 93-105) BEFORE `tunnelDial`.** An engineer running `postern tunnel device-1 --max-lifetime -30m` causes a full ssh.conf read + reaper sweep + atomic write (if any stale blocks were reapable) before the CLI surfaces `ErrTunnelNegativeLifetime`. Practical impact is nil — the reaper is idempotent + safe — but the ordering is mildly surprising on the failure path: a `--help`-style boundary check that lands deep into the file-mutation surface. The test `TestTunnelOpenMaxLifetimeRejected` correctly asserts `proxy.startCount() == 0` but doesn't observe the reaper side-effect. Future cleanup: hoist `lifetimeMinutesFromDuration` (or a new boundary-only `validateTunnelFlags` helper) to before the reaper call so flag errors short-circuit cleanly without file I/O. Pairs naturally with the `runTunnelReap` short-circuit (which already lives ahead of the device-id validation). Non-blocking — cosmetic ordering. | open |
| F-TN-F-5 | tunneling/TN-F | **Lifecycle ordering hole: in the `RemoveEphemeral`-fails-but-`teardownTunnel`-succeeds path (`pkg/cliapp/tunnel_open.go:152-161`), the proxy is torn down AFTER the failed ephemeral remove, even though spec D13 + LD-115 + the engineer's architectural note pin the order as "RemoveEphemeral BEFORE proxy.Close+Wait so a stale stanza doesn't mislead engineer's next `ssh <device>`".** Walk the failure path: cancel fires → `RemoveEphemeral` runs first (correct order on the happy path) → if it errors, code calls `teardownTunnel` and returns the combined error. The ephemeral stanza survives in ssh.conf pointing at the just-closed proxy port. Engineer's next `ssh <device>` reads the stanza, connects to 127.0.0.1:<port>, gets a connection-refused (the proxy closed) — exactly the failure mode LD-115's ordering invariant was meant to prevent. The defense-in-depth is the next `postern tunnel` invocation's reaper (which only fires on a dead pid; this is alive-pid-but-corrupt-state). Fix is small: if `RemoveEphemeral` fails, the engineer needs a clear signal that the stanza is now stale — either (a) attempt a second `RemoveEphemeral` after a brief sleep (covers transient EBUSY / EAGAIN), (b) write a fresh stanza marking the device as known-stale (e.g. set `Port 0` so any ssh attempt fails fast with a clean ssh-side error), or (c) document the failure mode in the engineer-facing error so they know to inspect ssh.conf manually. (c) is the cheapest and most honest. Non-blocking — `RemoveEphemeral` errors are themselves uncommon (the file was just successfully read + matched; the most likely failure is the F-TN-F-1 corrupt-RESTORE path). | open |
| F-TN-E-1 | tunneling/TN-E | **`iot:OpenTunnel` IAM grant has a latent fail-open property when `destinationConfig.thingName` is omitted on the SDK call.** The `iot:ThingName` `StringLike` condition (`terraform/postern-broker/iam.tf:94-101`) is evaluated only when AWS receives `destinationConfig.thingName` in the request — IAM treats missing context keys as "condition not applicable; statement matches" rather than as a denial. Today this is safe because `internal/tunneling/awsiot.go:75-76` always passes `DestinationConfig.ThingName` (via `request.ThingName`, populated from broker policy preamble that resolves the device record), but a future refactor that introduces a "default destination" path or accidentally omits the field would silently widen the grant to ANY `arn:aws:iot:*:*:tunnel/*` resource (i.e. cross-fleet thing names). Defensive hardening options: (a) add a `iot:OpenTunnel`-side check in `internal/tunneling/awsiot.go` that fails-closed if `request.ThingName == ""` (pairs with the IAM condition for defense-in-depth); (b) the Terraform side could add a `Bool` condition `aws:RequestTag/whatever` to enforce ThingName-presence — but AWS IoT secure tunneling doesn't expose a ThingName-presence condition key, so option (a) is the only practical defense. Pairs with the broader observation that the `tunneling_thing_name_prefix` default `"device-*"` is a useful narrowing but not a hard ceiling. Non-blocking; the broker code is correct today and a regression would have to reach `awsiot.go` to trip it. | open |
| F-TN-E-2 | tunneling/TN-E | **`tunneling_thing_name_prefix` variable lacks validation that enforces a non-empty `*` suffix (or at least rejects values without a wildcard).** The variable defaults to `"device-*"` (correct), and the documentation says "Set to empty to drop the condition entirely (broader grant; not recommended outside evaluation)" (correct). But an operator who sets `tunneling_thing_name_prefix = "device-"` (without the `*`) ends up with a `StringLike` IAM condition that matches the literal string `"device-"` — and AWS rejects nearly every real `iot:OpenTunnel` call because no `destinationConfig.thingName` value actually equals `"device-"` (real values are `device-S1234`, `device-prod-42`, etc.). The Terraform applies cleanly, `terraform plan` shows the IAM policy as expected, and the first real `postern ssh --tunnel <device>` invocation fails with an AWS `AccessDeniedException` that doesn't surface the cause clearly. Cheap fix: add a validation block on the variable asserting `var.tunneling_thing_name_prefix == "" || endswith(var.tunneling_thing_name_prefix, "*")`. Documents the gotcha at variable-declaration time and turns a runtime-only failure into a `terraform validate` failure. Non-blocking; user-error footgun, not a defect in the engineer's implementation. | open |
| F-TN-E-3 | tunneling/TN-E | **The integration test (`internal/securetunnel/integration_test.go`) does not exercise the `/ssh/tunnel` *denial* paths.** Spec §8 success criterion 2 covers both happy-path JSON shape AND denial-path audit shape; criterion 3 covers the `tunnel_authorized` + `tunnel_denied` `signer_failure`-pattern (here: `tunneling_unavailable` pattern) pair for AWS-call failures. The integration test asserts the happy-path two-event pattern + invariant T no-token-leak; the denial paths (policy DENY, registry unavailable, tunneling-call AWS error) are covered piecemeal in TN-A's handler tests and TN-C/D's CLI tests but not joined end-to-end through the same brokerclient → handler → real-issuer → fake-Tunneling pipeline this integration test wires up. Adding a `TestTunnelingEndToEnd_TunnelingError` subtest (fake Tunneling returns `tunnelingUnavailableErr`; assert single-engineer-visible 503 + audit `tunnel_authorized` + `tunnel_denied` joined on JTI + invariant T) would round out the cross-package proof. Non-blocking — the per-layer tests cover the failure shapes; this is "the integration test only proves the happy path" rather than a gap in correctness coverage. Two complementary subtests could ship in the same file with minimal duplication (the fakes already exist; only the `Tunneling.OpenTunnel` return value flips). | open |
| F-TN-E-4 | tunneling/TN-E | **`docs/tester-baselines.md` was not refreshed for TN-E** — the "Last gate" line at line 11 still reads "2026-05-14 TN-F close: untagged 550 RUN / ... combined `make check` RUN = 586". Independent re-run confirms TN-E adds 1 RUN (`TestTunnelingEndToEnd_HappyPath`) to bring the count to 587 / 549 PASS / 0 FAIL / 2 SKIP untagged + 36 RUN tagged = 587 combined (matches engineer's claimed count exactly). Spec §6 TN-E row item (7) names "tester baselines refreshed" as an explicit deliverable for this sub-phase. Engineer-flagged in their handoff that CHANGELOG.md isn't hand-edited because release-please regenerates it — same rationale doesn't apply to tester-baselines (no autogen). Most likely the tester-role refresh is expected as a sibling step post-engineer-handoff in this team's `phased-delivery` flow, in which case this observation is informational (tester will handle); if not, the engineer needs to refresh the file with the TN-E numbers before phase-close. Same applies to the "Test baseline at rest" line in `docs/team-handoff.md:38` which still reads 281 RUN (a much older baseline that pre-dates the engineer-CLI phase). Non-blocking on code/Terraform; documentation freshness gap. | open |

## Convention-violation tracking

| id | violation | files affected | resolution plan |
|----|-----------|----------------|-----------------|
| CV-1 | Production-code comments reference phase identifiers (`LD-64`, `LD-65`, `LD-70`, `D7`). User Go-style rule (`feedback_go_code_style.md`): "comments explain WHAT and WHY when non-obvious, NEVER reference phases / LD-X / sub-phase numbers". The pattern is also new — no other production `.go` file in `internal/` or `pkg/` references LD-XX. Test files have prior precedent (`F-BROKER-1`); production code didn't, until TF-A. **TF-A re-gate update**: the refactor compounded CV-1 by adding 6 new LD-XX references in production code (`preamble.go:22,28,165,226` — LD-83 ×2, LD-65, LD-84; `sshcert.go:92,213` — LD-83+LD-64, LD-84; `issuetimepayload.go:91,105,213` — LD-83, LD-82 ×2). Total CV-1 footprint in production code is now 13 references across 4 files. Refactor was not scoped to fix CV-1, but the addition increases the cleanup surface. **TF-B update**: 4 new LD-XX references in TF-B production files (`cmd/timefix-apply/capubpath_prod.go:5` — LD-69; `cmd/timefix-apply/capubpath_testbuild.go:13` — LD-69; `cmd/timefix-apply/main.go:42` — LD-69; `cmd/timefix-apply/verify.go:42` — LD-68) plus 1 new reference in Makefile (`Makefile:34` — LD-69 in the test-target comment) and 1 new reference in test file (`cmd/timefix-apply/verify_test.go:386` — LD-69). Total CV-1 footprint is now 17 production-code refs + 1 Makefile ref + 1 new test-file ref across 8 files. **TF-C update**: 7 new LD-XX/invariant-letter refs in TF-C production code (`cmd/timefix-set-clock/main.go:10` — LD-67; `main.go:17` — invariant O; `main.go:24` — invariant O; `main.go:28,84,130` — LD-77 ×3; `main.go:43` — LD-85 + D11; `main.go:55,102` — invariant P + invariant O; `cmd/timefix-set-clock/syscalls_linux.go:35` — LD-77) plus 5 new refs in TF-C test file (`main_test.go:45` — invariant O; `main_test.go:101` — LD-85 + D11; `main_test.go:171,182` — LD-77 ×2). Total CV-1 footprint is now 24 production-code refs + 1 Makefile ref + 6 test-file refs across 10 files. **TF-D update**: 2 new production-code refs (`pkg/cliapp/timefix.go:63` — LD-72 + LD-78; `pkg/cliapp/runtime.go:87` — invariant P, weaker form) plus 4 new test-file refs (`pkg/cliapp/timefix_test.go:161,193,201,243` — LD-78 ×4). Total CV-1 footprint was 26 production-code refs + 1 Makefile ref + 10 test-file refs across 12 files heading into TF-E. **TF-E sweep result (reviewer-verified 2026-05-13)**: `grep -rnE '//.*(LD-[0-9]+\|TF-[A-Z]\|invariant [A-Z]\|OQ-TF-[0-9]+\|D[0-9]+ )' --include='*.go' internal pkg cmd` filtered through `grep -v _test.go` returns ZERO hits. All 26 LD-X / D-X / TF-X / OQ-TF refs in production code were either inlined to the underlying rationale or deleted alongside dead code (e.g. `DenyReasonPrincipalTypeUnimplemented` removed when D12 made it dead). The Makefile ref at `Makefile:34` was also cleaned. **Residual production-code letter-refs**: 2 surviving `invariant [A-Z]` references at `cmd/timefix-apply/verify.go:66` and `:178-179` ("invariant-P purposes" and "per invariant R forward-compat rule"). These are weaker violations than LD-X refs (invariant names are spec-stable rather than phase-ephemeral) but per spec §8 success criteria #16 the production-code reference count should be zero. Documented as forward-looking obs 28 for cheap follow-up. **Test-file refs surviving** (lower priority per spec): `cmd/timefix-set-clock/main_test.go:45,101,171,182` (invariant O, LD-85, LD-77 ×2); `cmd/timefix-apply/verify_test.go:126,128,229,356,388,439` (invariant P ×4, invariant N, LD-69); `pkg/cliapp/timefix_test.go:36,68,578` (D12 ×3). Documented as forward-looking obs 29 for opportunistic cleanup. **Status**: production-code LD-X / D-X / TF-X / OQ-TF count = 0; production-code invariant-letter count = 2; Makefile = 0; test-file refs = ~13 (lower priority). Spec §8 #16 success criterion is met if interpreted as "no LD-X / phase / sub-phase number" wording strictly; needs a tiny follow-up edit to verify.go if interpreted to include invariant-letter refs. | sweep effectively complete; reduce remaining 2 production-code invariant-letter refs (verify.go:66, verify.go:178) opportunistically in a follow-up edit. Test-file refs deferred. |

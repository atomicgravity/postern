# Reviewer observations — Postern

> Numbered forward-looking observations from gate reviews. Updated by
> reviewer at end of each dispatch. Read by reviewer on every spawn;
> read by orchestrator when planning the next sub-phase brief.
>
> Forward-looking = "things nicer at a future extraction point, pattern
> duplications worth consolidating, convention violations to address
> symmetrically." NOT blocking issues — those stay in the gate verdict.

## Items

Reset 2026-06-08 with the scaffolding reconstruction. Prior phase observations
were retired with the May-15 doc cleanup; the survivors that still matter are
the deferred audit items, tracked in `docs/team-handoff.md` §8 and below.

| id | first raised | item | status |
|----|--------------|------|--------|
| O-1 | 2026-05-15 (audit) | S-2 tunnel-mode host-key trust — decide acceptability vs SSH host certs | deferred |
| O-2 | 2026-05-15 (audit) | BD-4 JWKs-fetch DoS pre-rate-limit | deferred |
| O-3 | 2026-05-15 (audit) | BD-3/BD-5 reject over-length / empty OIDC `sub` | deferred |
| O-4 | client-auth/A | Predicate→rule mapping lives in two places: `predicateCount()` (brokerhandlers config) and the `switch` in `brokerwire.principalClassRule`. When B/C touch rules, consider one config→verifier translation owning both the one-of contract and the kind selection so they can't drift. | open |
| O-5 | client-auth/A | `DefaultPrincipalClass`/`MaxPrincipalClassRunes` are duplicated as consts in both `internal/idp` and `pkg/brokerhandlers` (independent values that happen to agree). If they must stay in lockstep, a shared source prevents silent divergence; if intentionally independent, a one-line note documents the non-coupling. | open |
| O-6 | client-auth/A | `claimPresent`/`claimEquals` carry `[]string` branches that are dead for raw-JSON-map input (arrays decode to `[]any`). Harmless defensive code; candidate for trimming to the live `[]any` path if no typed-map caller arrives. | open |
| O-7 | client-auth/A | Verifier rule eval is unit-tested only via full `VerifyAccessToken` round-trips (sign + JWKS fixture). If predicate forms grow, a table test directly over `rule.matches` on a raw map would isolate predicate logic from the OIDC fixture. | open |
| O-8 | client-auth/B | `DenyReasonMaxLifetimeExceedsCeiling` ("max_lifetime_exceeds_ceiling") is now reused by the cert path for a *negative* request, where nothing exceeds a ceiling. The shared literal keeps operator dashboards uniform, but the reason string no longer describes the cert-path trigger. If a future phase wants accurate reasons, a distinct `max_lifetime_negative` literal would split them; for now the reuse is the documented tunnel-mirror choice. | open |
| O-9 | client-auth/B | The requested-TTL clamp is now implemented twice with near-identical shape: cert (`sshcert.go`, `requested>0 && requested<ceiling`) and tunnel (`tunnel.go`, default-substitute + over-ceiling clamp). The two differ (cert clamps over-ceiling silently; tunnel's ceiling is a fixed const) but share the negative-reject + resolved-value-to-context contract. A shared `resolveRequestedLifetime` helper returning `(resolved, denyReason)` would prevent the two from drifting as policy-context keys multiply. | open |
| O-10 | client-auth/B | No test pins the exactly-at-ceiling boundary (`requested == ceiling` → ceiling honored, not clamped). The current "below ceiling" (120m vs 12h) and "above ceiling" cases bracket it but leave the `<` vs `<=` edge unguarded; a one-line subtest at the boundary would lock the off-by-one. | open |
| O-11 | client-auth/B | The `int32 minutes → time.Duration` conversion is overflow-safe only incidentally: a `MaxInt32` request wraps negative or past-ceiling and the `>0 && <ceiling` guard rejects it (verified by reviewer). No test asserts this, and a future refactor of the guard could reintroduce a wrap-to-sub-ceiling bug. A boundary test at `math.MaxInt32` minutes (expect clamp-to-ceiling) would lock the property. | open |
| O-12 | client-auth/C | The six-field caller-identity block (`EngineerSub`/`EngineerEmail`/`EngineerGroups`/`PrincipalClass`/`ClientID`, + Timestamp/JTI/SourceIP/UserAgent) is now hand-copied into six `AuditEvent` literals (preamble `verifyEngineer` + `resolveDevice` templates, `denialTemplate`, and the three authorized-row builders in sshcert/tunnel/issuetimepayload). Each new caller-scoped audit field (this phase added two) must be threaded into all six by hand; a missed site silently drops the field on that row. A `callerAuditFields(caller, meta)` helper returning the populated common subset would make "add an identity field" a one-site change and prevent per-row drift. | open |
| O-13 | client-auth/C | Stamping correctness across the issued/denied rows rests entirely on the struct-copy idiom (`issued := authorized`, `denied := authorized`) in sshcert/tunnel and on `denialTemplate` in time-payload's signer-failure path — none of which is asserted to *derive* from the authorized template. The new per-pipeline tests assert the fields are present on every emitted row (good), but an independent reconstruction of an issued row that forgot the copy would only be caught if that pipeline's loop-over-all-events test still ran. The time-payload signer-failure deny (the one row built via `denialTemplate` rather than copy) has no test exercising its principal_class/client_id stamping specifically; a signer-failure subtest asserting both fields would lock the lone non-copy deny path. | open |
| O-14 | client-auth/D | `wrapTokenError` (shared with the PKCE path) appends the IdP's response body to client-credentials token errors. The secret lives in the *request* form, not the response, so it isn't echoed — but for client-credentials the response body is the only error context an operator sees, and a misbehaving/proxying IdP could in principle reflect request params. No test pins that the surfaced error excludes secret material. A negative assertion (token-error test asserting the wrapped error does not contain the secret value) would lock invariant T on this path against an IdP that reflects input. | open |
| O-15 | client-auth/D | The two oauthlogin entrypoints (`ClientCredentialsToken`, `Login`/`AccessToken`) now each re-implement the same discovery → `normalize`/`validate` → token-mint → `accessTokenExpiry`-sanity shape with their own near-identical option structs and `normalize`/`validate` helpers. The duplication is currently small and the divergence (no refresh token, no callback) is real, so splitting now would be premature; flag for consolidation only if a third grant arrives. | open |
| O-16 | client-auth/D | The in-process re-mint-on-demand lifecycle the spec describes ("or caches in memory for the process") is implemented as mint-every-call: `defaultAccessToken` on the client-credentials branch performs a full discovery + token request on each invocation, with no per-process memo. For multi-call sessions (e.g. a wrapper issuing `ssh` then `scp`) this is two discoveries + two mints. Harmless and correct; a process-lifetime in-memory cache keyed by profile would cut redundant IdP round-trips if call-volume grows. | open |
| O-17 | client-auth/E | There are now two near-identical `time.Duration → int32 minutes` converters in `pkg/cliapp`: `lifetimeMinutesFromDuration` (tunnel, `tunnel.go`, enforces the 12h AWS cap) and `certLifetimeMinutesFromDuration` (cert, `certflow.go`, no client-side cap — broker clamps per-class). They share the zero-passthrough + round-up-to-1 + negative-reject shape and differ only in the cap. The divergence is real (the cert path *must not* impose a ceiling), so unifying now risks re-baking a cap into the cert path; flag for a shared `(d, cap)` core only if a third TTL flag arrives. | open |
| O-18 | client-auth/E | "Absent flag → broker uses ceiling" rests on two independent zero-values agreeing: `DurationVar` default `0` → `certLifetimeMinutesFromDuration(0)` returns `0` → `MaxLifetimeMinutes` int32 zero-value → omitted on the wire. `TestMintCertMaxLifetimeOmittedLeavesZero` pins the CLI→request leg; no cliapp test pins that request-zero actually serializes as omitted/ceiling-substituted (that contract lives broker-side, sub-phase B). Cross-layer the property is covered, but a one-line wire-encoding assertion (zero MaxLifetimeMinutes marshals absent) would keep the omit semantics from silently flipping if the field gains `omitempty`-sensitive tags. | open |
| O-19 | client-auth/E | `certMaxLifetimeMinutes int32` is now threaded positionally through `mintAndCache` / `ensureFreshCert` / `tunnelDial` alongside the pre-existing `refresh bool` / `verbose bool` positional tail. Three call sites pass a bare `0` literal (`addhost.go`, `tunnel_open.go`) or interleave it among bools; the params are type-distinct enough that a swap would compile-fail today, but as the cert-request knob set grows (B's per-class context, future class hints) a small `certRequestOpts` struct threaded through these helpers would stop the positional list from becoming a swap hazard. | open |
| O-20 | client-auth/F | `setPrincipalClassesFromEnv` replaces the whole file-provided block when the env var is set (`config.IDP.PrincipalClasses = &parsed`) — there is no merge. A deployment that ships a file with rules *and* sets `POSTERN_IDP_PRINCIPAL_CLASSES` for the default silently loses the file rules (only `POSTERN_IDP_PRINCIPAL_CLASS_DEFAULT` merges-on-top). Correct for the Lambda (no file) and documented in the comment as "replaces any file-provided block," but the replace-vs-merge asymmetry between the two principal-class env vars is a latent surprise for a mixed file+env deployment; a `--print-config` example showing the resolved block would make the precedence legible. | open |
| O-21 | client-auth/F | `schema.json` now hard-couples `principal_class: required` to the broker unconditionally emitting it in `contextMap` (verified honest). The honesty is enforced only by reading both sites together — no test asserts "broker always emits principal_class" against "schema requires it." If a future refactor makes the class conditionally emitted (e.g. omit-on-empty, mirroring `client_id`), STRICT AVP validation breaks only at `terraform apply`, not in `make check` (the same gap the O-37/commit-msg rationale calls out). A contextMap test asserting `principal_class` is present even for an empty `Caller.Class` would lock the required:true contract in the Go suite. | open |
| O-22 | client-auth/F | The principal-classes block is the first env var carrying a structured multi-field document (vs the existing scalar/CSV env overrides). `yaml.Unmarshal` into `PrincipalClassesConfig` silently ignores unknown keys (no `KnownFields(true)`), so a typo'd predicate key in the IaC `jsonencode` (e.g. `claim_abset`) parses to an all-empty rule and is then caught by `validatePrincipalClasses` as a zero-predicate rule — actionable, but the error names the predicate contract, not the typo'd key. If structured env documents proliferate, a strict decoder on this path would point operators at the bad key directly. | addressed-in-G |
| O-23 | client-auth/G | Strict decoding (`KnownFields(true)`) now applies to the env path (`setPrincipalClassesFromEnv`) but not to the file path (`LoadConfig`, which decodes without `KnownFields`). For the Lambda (env-only) this closes O-22; for a file-based deployment a typo'd `principal_classes` key still silently falls back. The scoping is defensible (env is the primary principal_classes path on the Lambda, and flipping the whole file decoder to strict risks rejecting existing configs with unrelated extra keys), but the strict/lenient split now lives across two decoders for the same struct. If file-side typo protection is later wanted, scoping strictness to just the `principal_classes` sub-document on the file path (rather than the whole `Config`) would extend the protection without the blast radius of a fully strict file decoder. | open |
| O-24 | setup-ssh | `setupSSHCommand` re-derives the config path twice through different helpers: the present/idempotency check goes via `checkIncludePresence` → `userSSHConfigPath`, while the write goes via a second direct `userSSHConfigPath` call in `runSetupSSH`. They agree today (both `$HOME/.ssh/config`), but the "which file do we touch" decision now lives in two call sites. A single resolved path threaded from one `userSSHConfigPath` call into both the presence check and `prependInclude` would make the read-target and write-target provably the same file. | open |
| O-25 | setup-ssh | Idempotency rests on `checkIncludePresence`, which matches an `Include` line *anywhere* in the file — including one that sits *after* a `Host`/`Match` block (where ssh scopes it to that block, so it is not globally effective). Such a config is reported "already present" and `setup-ssh` no-ops, leaving the engineer with a non-global Include and no prompt to fix it. Acceptable v1 limitation (the prepend path produces a correct global Include; only a hand-edited mis-scoped Include hits this), flagged forward-looking: a "present but not at global scope" detection could warn rather than silently no-op. | open |
| O-26 | setup-ssh | `prependInclude` reads the whole existing config into memory and re-writes it with the line prepended every run that isn't a no-op. Correct and atomic for the expected small-file case; if a future caller ever invokes it on an already-Include-bearing file by a path that bypasses `checkIncludePresence`, it would prepend a duplicate. The presence gate in `runSetupSSH` is the only thing preventing duplication — a guard inside `prependInclude` itself (skip if the first non-comment line is already the Include) would make the function safe independent of its caller. | open |
| O-27 | broker-aud-scope | The fail-closed correctness of the merged check rests entirely on the `matched* := field != "" && match` form in `VerifyAccessToken`; nothing structurally prevents a future refactor from regressing it to `field == "" || match` (the exact fail-open trap the spec names). The test suite catches the regression (verified by mutation: single-config-wrong, exact-scope, and fail-closed-neither cases flip to FAIL under the buggy form), so it is guarded — but the safety is a property of two parallel boolean expressions a reviewer must read together, not a single chokepoint. A tiny `matchesConfiguredProof(field, fn) bool` helper returning `field != "" && fn()` would make the fail-closed semantics live in one named place rather than being re-derived per field. | open |
| O-28 | broker-aud-scope | The OR matrix proves cross-app isolation at the verifier boundary, but the `cross-app/other-app-token` adversary is modeled with a *plausible* other-app token (different `aud` + different `scope`, same issuer/sig) rather than a *replayed real* App-B token — there is no shared-pool integration test exercising two AVP identity sources / two app-client audiences end-to-end. Faithful at the unit layer and sufficient for this gate; flagged forward-looking because the security claim ("a token issued for App B cannot mint a cert via App A's broker") ultimately depends on the broker's `aud`/`scope` config matching the IdP's real per-app issuance, which only an integration fixture (or the Cognito sample's documented audiences) can pin. | open |
| O-29 | broker-aud-scope | Matrix gap (no security hole, completeness only): with **both** fields configured, every adversarial case sets the wrong `aud` together with the wrong/absent `scope`, and every accept case satisfies at least one. There is no case asserting an `scp`-array-only proof under both-configured (all both-config accept cases use the space-delimited `scope` string), and no case where the configured scope appears as a **substring of a granted scope under both-config** (the prefix/suffix collisions live only in the scope-only `TestVerifyAccessTokenExactScopePrecision` table). Exact-match is exercised, so this is redundant-coverage rather than a missing guard — but a both-configured `scp`-array accept and a both-configured prefix-collision reject would close the cross-product. | open |
| O-30 | broker-aud-scope | No case covers a **whitespace-only / malformed scope claim** under both-configured (e.g. `scope: "   "` with wrong `aud`). `hasScope` runs `strings.Fields`, which yields no tokens for whitespace-only input, so the path correctly rejects — but it is untested, and the empty-string case (`or/empty-string-aud-and-scope-claims`) only covers `scope: ""`, not `"   "`. Low risk (same code path), noted as a one-line matrix addition to lock that `strings.Fields` whitespace handling never regresses into treating a blank scope as a wildcard proof. | open |

(status: open / addressed-in-<sub-phase> / deferred / moot)

## Convention-violation tracking

None open. The CV-1 phase-identifier comment regression (audit Q-3/G-1/G-2) was
swept to 0 references and verified clean on 2026-06-08.

| id | violation | files affected | resolution plan |
|----|-----------|----------------|-----------------|
| — | — | — | — |
